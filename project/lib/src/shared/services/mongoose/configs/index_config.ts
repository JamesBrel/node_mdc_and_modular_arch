class IndexConfig {
  /**
   * createIndex
   */
  public static async createIndex() {
    // await UserModel.createIndexes(); => to create the index of user model , so available it
  }
}

export {IndexConfig};
